Create a new Expo React Native app called BillMinder. Set up the project with the following structure: a main App.js entry point, a screens/ folder containing HomeScreen.js, AddBillScreen.js, and BillDetailScreen.js. Use React Navigation for moving between screens. The app should use a clean, professional colour scheme with dark navy blue (#0F3460) as the primary colour and white as the background. Install all required dependencies.

Build the HomeScreen for BillMinder. It should include: a header at the top showing the app name and the current month. Below the header, a summary card showing the total amount due this month and whether anything is overdue. Below that, a scrollable list of bills. Each bill row should show a coloured indicator dot (red for overdue, orange for due within 7 days, green for paid), the bill name, the due date, and the amount. At the bottom right, add a floating ‘+’ button for adding a new bill. Use sample data for now with at least 4 bills: Rent $1,200 due March 25, Netflix $15 due March 22, Credit Card $430 due March 28, Internet $65 due March 30.

Build the AddBillScreen for BillMinder. It should be a form with the following fields: Bill Name (text input), Amount in dollars (numeric input), Due Date (a date picker), Repeat frequency (a dropdown with options: One-time, Weekly, Monthly, Yearly), Category (a dropdown with options: Housing, Transport, Subscriptions, Insurance, Utilities, Credit Cards, Other), and Remind me (a dropdown with options: On the day, 1 day before, 3 days before, 7 days before). At the bottom of the form, add a ‘Save Bill’ button in navy blue. Add a back arrow at the top left to return to the dashboard. Make the form scroll if it does not fit on screen.Don’t allow saving if the name is empty. Don’t allow saving if the amount is not a valid numeric amount.


Build the BillDetailScreen for BillMinder. When a user taps a bill on the HomeScreen, they should be taken to this screen. It should show: the bill name as a large heading, the amount, the due date, the repeat frequency, the category, and the reminder setting. Below the bill details, show a ‘Payment History’ section with a list of past payments (use sample data showing the last 3 months marked as Paid). At the bottom of the screen, show two buttons: a green ‘Mark as Paid’ button that records the payment and returns to the dashboard, and a grey ‘Snooze 3 Days’ button that pushes the reminder back. Add a back arrow at the top to return to the dashboard.

Wire up the navigation for BillMinder so that: tapping the ‘+’ button on HomeScreen opens AddBillScreen, tapping any bill row on HomeScreen opens BillDetailScreen with that bill’s data, tapping ‘Save Bill’ on AddBillScreen adds the bill to the list and returns to HomeScreen, and tapping ‘Mark as Paid’ on BillDetailScreen updates that bill’s status and returns to HomeScreen. Add a bottom navigation bar with three tabs: Home, Add Bill, and Stats (Stats can be a placeholder screen for now). Make the active tab highlight in navy blue. Ensure the transitions between screens feel smooth.

chapter 5

Add persistent local storage to BillMinder using @react-native-async-storage/async-storage. Install the package and update the app so that: when the app loads, it reads the saved list of bills from AsyncStorage and populates the dashboard. When a new bill is added via the AddBillScreen, it is saved to AsyncStorage. When a bill is marked as paid on the BillDetailScreen, the updated bill is saved back to AsyncStorage. The data should be stored as a JSON string under the key 'billminder_bills'. If no data exists yet, the app should start with an empty list.


Add a delete option to BillDetailScreen in BillMinder. On the bill detail screen, add a ‘Delete Bill’ option — either as a button at the bottom of the screen or as a trash icon in the top right corner. When tapped, show a confirmation dialog asking ‘Are you sure you want to delete this bill?’ with Cancel and Delete options. If confirmed, remove the bill from AsyncStorage and navigate back to the HomeScreen. The bill should no longer appear in the list.


Chapter 6 

Add push notification support to BillMinder using expo-notifications. Install the package. When the app first launches, request permission from the user to send notifications — on iOS this shows a system dialog, on Android it should be granted automatically. Store whether permission was granted. If permission is denied, show a small banner on the HomeScreen saying ‘Enable notifications in Settings to get bill reminders’. Set the notification handler so notifications display as alerts with sound and badge updates when the app is in the foreground.

Update BillMinder so that when a bill is saved via AddBillScreen, a local notification is scheduled based on the bill’s due date and reminder setting. The notification should fire at 9am on the reminder day. For example, if a bill is due March 25 and the reminder is set to 3 days before, schedule a notification for March 22 at 9am. The notification title should be the bill name followed by ‘is due soon’. The body should show the amount and the exact due date. Store the notification ID alongside the bill in AsyncStorage so it can be cancelled later if the bill is deleted or marked as paid. When a bill is deleted or marked as paid, cancel its scheduled notification using the stored ID.


Update BillMinder’s notifications to include two action buttons: ‘Mark as Paid’ and ‘Remind Me Tomorrow’. When the user taps ‘Mark as Paid’ from the notification (without opening the app), the bill should be updated to paid status in AsyncStorage and the notification should be cancelled. When the user taps ‘Remind Me Tomorrow’, cancel the current notification and schedule a new one for the same time the following day. Use expo-notifications’ notification categories to define these actions.


Chapter 7 

Apply a consistent colour system across BillMinder. Use the following palette everywhere: primary brand colour #4660AF (medium blue) for the header, primary buttons, and active states; danger colour #D94040 (red) for overdue bills and delete actions; success colour #2EA05A (green) for paid bills and positive confirmations; warning colour #D48228 (amber) for bills due within 3 days; background #F5F6FA (near white) for all screens; card background #FFFFFF (white) for bill cards with a subtle shadow. Update every screen — HomeScreen, AddBillScreen, and BillDetailScreen — to use these values. Replace all hard-coded grey colours. Define the colours as constants in a theme.js file so they can be reused.

Update BillMinder’s typography across all screens. On the HomeScreen bill list, show the bill name in bold at 17pt and the due date beneath it in regular weight at 13pt using the muted grey #8A8FA8. Show the amount right-aligned in bold at 17pt, coloured using the urgency palette: #D94040 if overdue, #D48228 if due within 3 days, #2EA05A if paid, and the brand blue #4660AF otherwise. On the AddBillScreen, use 15pt bold labels above each input field. On the BillDetailScreen, use 22pt bold for the bill name at the top, 16pt regular for the amount, and 13pt muted for all metadata rows. Ensure all font sizes are defined as constants in theme.js alongside the colours.

Add icons throughout BillMinder using @expo/vector-icons (Ionicons set). On the HomeScreen, replace the plain ‘+ Add Bill’ text button with a floating action button (FAB) — a round blue circle containing a white ‘add’ icon, fixed to the bottom-right corner of the screen. In the bill list rows, add a small calendar icon before each due date and a chevron-right icon on the far right of each row to indicate the row is tappable. On the BillDetailScreen, add a tick-circle icon next to the ‘Mark as Paid’ button and a trash icon next to the ‘Delete’ button. In the header, add a bell icon in the top-right corner that, when tapped, navigates to a simple notifications settings screen (just a placeholder for now).

Redesign the bill list on HomeScreen to use elevated card-style rows instead of flat coloured strips. Each card should be a white rounded rectangle (border radius 12) with a subtle shadow (elevation 2 on Android, shadowColor black at 10% opacity on iOS). Inside each card, add 14px vertical padding and 16px horizontal padding. Add a coloured left border (4px wide) using the urgency colour — red for overdue, amber for due within 3 days, blue for upcoming, grey for paid. Between cards, use a 10px gap. On all screens, increase the horizontal screen padding from whatever it currently is to 16px on each side. Give the AddBillScreen input fields a white background with a 1px light grey border and 8px border radius. Add 20px spacing between each form field.

Add a friendly empty state to the HomeScreen for when the user has no bills yet. In the centre of the screen, show a large receipt or wallet icon (from Ionicons, in the brand blue colour at 80px size), a heading in bold 20pt text that says ‘No bills yet’, and a subtitle in 15pt muted grey that says ‘Add your first bill and BillMinder will remind you before it’s due.’ Below that, show a blue button labelled ‘Add Your First Bill’ that navigates to AddBillScreen. Also add a brief loading indicator (ActivityIndicator from React Native) that shows for half a second while bills are being loaded from AsyncStorage on app launch, so the empty state does not flash before the data appears.

Chapter 8

Add horizontal filter chips to HomeScreen, below the notification banner and "Due this month" card, above the bill list.Four chips — icon (Ionicons 20pt) + count only, no text labels. All uses layers-outline and shows total bills. Upcoming uses today-outline and shows unpaid bills due today or later. Overdue uses alert-circle-outline and shows unpaid bills past due. Paid uses checkmark-circle-outline and shows bills marked paid.Active chip = brand blue background, white icon and count. Inactive = light card, muted colours. Overdue chip turns red when unselected and overdue bills exist; when selected it uses the same blue as the others. Chip min-height 44 so icons aren't clipped.Full accessibility labels on each chip, e.g. "Overdue bills, 3 bills". Filter updates the list instantly. Reset to All on screen focus via useFocusEffect. Empty state shows "No bills matching [filter name]."

Add a sort control to the HomeScreen in BillMinder, displayed as a compact row of pill buttons below the filter tabs. The sort options should be: Due Date (default, earliest due date first), Amount (largest amount first), and Name (A to Z alphabetically). The active sort option should be highlighted in the brand blue. Sorting should work in combination with the active filter tab — for example, when the user is on the Overdue tab sorted by Amount, only overdue bills appear, sorted by the largest amount first. The sort preference should persist while the user is on the HomeScreen but can reset to Due Date when they navigate away.

Add a monthly summary card to the top of the HomeScreen in BillMinder, displayed above the filter tabs. The card should be a rounded rectangle with a subtle blue tint background. It should display three figures for the current calendar month: ‘Due’ (the total of all bill amounts due this month, whether paid or not), ‘Paid’ (the total of bills marked as paid this month), and ‘Remaining’ (Due minus Paid). Show the month name and year as the card title (e.g., ‘March 2026’). Colour the Due amount in the danger red, the Paid amount in the success green, and the Remaining amount in the brand blue. If all bills for the month are paid, show a small green checkmark and the text ‘All clear!’ instead of the Remaining figure. Update the card in real time as bills are marked as paid or new bills are added.

Add a search bar to the top of the HomeScreen in BillMinder, above the monthly summary card. The search bar should be a rounded white input field with a magnifier icon on the left. As the user types, the bill list below filters in real time to show only bills whose name contains the search text (case-insensitive). While a search query is active, hide the filter tabs and sort controls — the search results replace them. Show a ‘Clear’ button (an X icon) on the right side of the search bar whenever text has been entered; tapping it clears the search and restores the full filtered list. If no bills match the search, show a centred message: ‘No bills found for “[query]”’ with a small magnifier illustration. Tapping away from the search bar (or pressing the keyboard’s return key) should dismiss the keyboard but keep the current search query active so the user can see the results.

Chapter 9

Add recurring bill support to BillMinder. On AddBillScreen, add a ‘Repeat’ field below the due date. It should be a dropdown or segmented picker with the options: Never (default), Weekly, Monthly, Quarterly, and Yearly. Store the recurrence setting alongside the bill in AsyncStorage. When a recurring bill is marked as paid, automatically create a new bill with the same name, amount, category, currency, and reminder settings, but with the due date advanced by the recurrence interval (7 days for weekly, 1 month for monthly, 3 months for quarterly, 12 months for yearly). Immediately schedule a notification for the new bill using the same reminder offset. On the HomeScreen, show a small repeat icon (the circular arrow from Ionicons) on the right side of any bill that has a recurrence set, so the user can see at a glance which bills are recurring. On BillDetailScreen, show the recurrence setting in the bill details and add an ‘Edit Recurrence’ option.

Add a category field to BillMinder. On AddBillScreen, add a ‘Category’ selector that shows six options as coloured pill buttons: Housing (blue #4660AF), Utilities (teal #14A096), Subscriptions (purple #823CC8), Insurance (amber #D48228), Transport (green #2EA05A), and Other (grey #8A8FA8). The selected category should be highlighted in its colour and all others shown as outline-only. Store the category with each bill. On the HomeScreen bill cards, show a small coloured category chip below the bill name displaying the category label. On BillDetailScreen, show the category as a coloured pill in the bill header. Add a ‘Category’ filter row above the bill list (below the sort controls), showing the six categories as compact pill buttons. When a category pill is selected, the bill list filters to show only bills in that category and the filter tab and sort controls still apply on top. Show the total monthly amount for the selected category below the filter row. Allow only one category filter to be active at a time; tapping the active one clears the filter.

Add per-bill currency support to BillMinder. On AddBillScreen, add a ‘Currency’ field that shows a horizontal scrollable row of currency options: USD ($), EUR (€), GBP (£), CAD ($), AUD ($), JPY (¥), MXN ($), BRL (R$), and INR (₹). Default to USD. Store the selected currency with each bill. Everywhere an amount is displayed — bill list cards, BillDetailScreen, notifications — show the correct currency symbol for that bill rather than a global symbol. On the HomeScreen monthly summary card, if bills use more than one currency, group the totals by currency and display them in a stacked format (e.g., Due: $1,200 USD / €25 EUR) instead of a single combined total. Add a default currency setting accessible from a Settings screen (create a simple Settings icon in the header that opens a minimal settings page) so the user can set their preferred currency once and have it pre-selected on new bills.

Chapter 10 

Add a Charts screen to BillMinder. Create a new ChartsScreen component and add it to the bottom navigation bar as a second tab, between the HomeScreen and the Settings tab. Use a bar-chart icon from Ionicons for the tab. The Charts screen should have the same header style as the HomeScreen — the branded gradient header bar with the title ‘Spending Insights’. For now the screen body can be empty with a centred placeholder text ‘Charts coming soon’. We will add the charts in the following prompts.


Add a category donut chart to the ChartsScreen in BillMinder. Install a suitable charting library compatible with Expo (such as Victory Native or react-native-gifted-charts). The chart should show the current month’s spending broken down by category as a donut (ring chart). Each slice should use the same colour as its category from theme.js: Housing blue, Subscriptions purple, Utilities teal, Insurance amber, Transport green, Other grey. Display the total monthly spending amount in the centre of the donut. Below the chart, show a legend with each category’s colour swatch, name, dollar amount, and percentage of total. Sort the legend entries from largest to smallest. If a category has no bills this month, omit it from the chart and legend. Show a month picker above the chart (left and right arrow buttons with the current month name between them) so the user can browse previous months.

Add a monthly trend bar chart below the donut chart on the ChartsScreen in BillMinder. The chart should show total spending for the past six months as vertical bars. Use the brand blue colour for historical months and the success green for the current month. Label each bar on the x-axis with a three-letter month abbreviation (Jan, Feb, Mar, etc.) and display the dollar total above each bar. If a month has no bills, show a zero-height bar with a ‘$0’ label. Below the chart, show a single line of summary text comparing the current month to the previous one: for example, ‘March: $2,250 — up 10% vs February’ in green if spending decreased or red if it increased. The chart should scroll as part of the main ChartsScreen scroll view, not as a separate scrollable component.

Add a progress ring and insights section below the bar chart on the ChartsScreen in BillMinder. The progress ring should be a circular ring that fills clockwise from the top, coloured green, showing the percentage of this month’s total bill amount that has been paid. Display the percentage in the centre of the ring as a bold number with a ‘% paid’ label below it. Below the ring, show three summary figures side by side in a row: total Due (red), total Paid (green), and Remaining (blue). Below that, add an Insights section with a heading and up to four auto-generated insight chips. Each chip is a small rounded pill with a coloured background. The insights to generate are: (1) if there are any overdue bills, show a red chip with the count, e.g. ‘2 overdue bills’; (2) show an amber or green chip comparing this month’s spending to last month with a percentage change; (3) show a blue chip identifying the largest spending category, e.g. ‘Housing is 48% of spending’; (4) if all bills are paid, show a green chip saying ‘All clear — nothing due!’. Only show insights that are relevant — do not show the overdue chip if there are no overdue bills.

Make the donut chart slices tappable. When the user taps a slice, navigate to the HomeScreen with the category filter pre-set to that category, so they can see the individual bills that make up that slice. Also, make the overdue insight chip tappable — tapping it should navigate to the HomeScreen with the Overdue filter tab active. This creates a natural ‘drill down’ workflow from the insight to the underlying data.


Chapter 11 

Add a three-screen onboarding flow to BillMinder that appears only on the very first launch. Use AsyncStorage to store a flag ‘hasOnboarded’ — if it is false or missing, show the onboarding; if it is true, go straight to the HomeScreen. The three screens should have a dark gradient background using the brand colours and be swipeable left and right. Screen 1: a large bill or wallet icon in the centre, heading ‘Never miss a bill’, subtitle ‘BillMinder reminds you before every due date — even when the app is closed.’ Screen 2: a checklist illustration with three rows — ‘Add your bills once’, ‘Get reminded automatically’, ‘Track your spending visually’ — each with a green tick icon. Screen 3: the heading ‘You’re ready to go’, subtitle ‘Set up your first bill now and BillMinder takes it from there.’ and a prominent white ‘Add My First Bill’ button that navigates to AddBillScreen and sets the hasOnboarded flag. Every screen also has a ‘Skip’ link in the top right that skips to HomeScreen and sets the flag. Show three dot indicators at the bottom to indicate progress through the screens.


Create a custom app icon and splash screen for BillMinder. For the icon: generate a 1024x1024 PNG file saved as assets/icon.png. The design should be a rounded rectangle with the brand blue gradient background (#4660AF to #0F3460), a bold white dollar sign in the centre with a small circular ring around it, and the letter ‘B’ subtly incorporated — clean, recognisable at small sizes. For the splash screen: generate a 1284x2778 PNG saved as assets/splash.png with the same dark blue gradient background, the app icon centred vertically, the word ‘BillMinder’ in white below it in a bold clean font, and three small loading dots beneath the name. Update app.json to point to both files: set ‘icon’ to ‘./assets/icon.png’, set the splash ‘image’ to ‘./assets/splash.png’ and ‘backgroundColor’ to ‘#0F3460’. Reload the app to confirm the splash screen appears on launch.

Add a data export feature to BillMinder’s Settings screen. Add an ‘Export Data’ row to the settings list. When tapped, show a bottom sheet with two options: ‘Save CSV File’ and ‘Email CSV’. The CSV should include all bills with the following columns: Name, Amount, Currency, Category, Due Date, Status (Paid or Unpaid), Paid Date (if paid), Recurrence, and Notes. The first row should be a header row with those column names. Format dates as YYYY-MM-DD. Use Expo’s expo-sharing library to trigger the native share sheet so the user can save the file to Files, AirDrop it, or email it depending on their device. Name the file ‘BillMinder_Export_YYYY-MM-DD.csv’ using today’s date. Show a confirmation toast message after the export is triggered: ‘Export ready — choose where to save it.’

Apply the following small polish improvements to BillMinder. (1) Haptic feedback: use Expo’s expo-haptics library to add a light haptic tap when the user marks a bill as paid and a medium haptic when they delete a bill — the physical feedback makes actions feel more satisfying and intentional. (2) Pull-to-refresh: add pull-to-refresh on the HomeScreen so the user can drag down to reload the bill list — a familiar gesture that also reassures users their data is up to date. (3) Keyboard avoidance: on AddBillScreen, ensure the form scrolls up when the keyboard appears so no input fields are hidden behind it — use KeyboardAvoidingView from React Native. (4) Loading states: add a skeleton loading state to the HomeScreen — grey placeholder rectangles in the shape of bill cards that show for half a second while data loads, instead of a blank screen. (5) App version: display the app version number at the bottom of the Settings screen in small muted text, e.g. ‘BillMinder v1.0.0’ — this helps with support and makes the app feel maintained.

Bonus

You just built [feature name]. Now try to break it. Think about: (1) what happens with missing or empty inputs, (2) what happens with very large amounts of data, (3) what happens if the user does things in an unexpected order, (4) what happens if the app is closed and reopened mid-task, and (5) what happens on an old or slow device. For each problem you find, fix it. Then tell me what you found and what you changed.


Try to break the bill storage in BillMinder by testing these edge cases: (1) Add a bill with no name — what happens? Can a nameless bill be saved? If so, fix it by requiring a name before saving. (2) Add a bill with a negative amount like -$50 — does it save? Should it? Add validation to prevent negative amounts. (3) Add a bill with a due date in the past — is there any warning shown? There should not be (past due dates are valid), just confirm it saves correctly. For each issue found, fix it and show me what changed.


Test BillMinder’s storage with a large number of bills. Add 50 bills programmatically — write a temporary ‘Load Test Data’ button in Settings that adds 50 fake bills with random names, amounts, and due dates. Then: (1) Check if the HomeScreen scrolls smoothly with 50 bills. (2) Close and reopen the app — does it load all 50 bills quickly or does it feel slow? (3) Delete all 50 bills and confirm storage is fully cleared. Fix any performance issues you find, then remove the test button.

Test what happens when the user does things in an unexpected order while using BillMinder storage: (1) Start adding a bill, fill in the name, then press the back button without saving — does a partial bill get saved accidentally? (2) Add a bill, immediately mark it as paid, then immediately delete it — does the storage stay consistent or are there orphaned records? (3) Add a recurring bill, mark it as paid (which creates the next instance), then delete the original — does the new instance remain correctly? Fix any inconsistencies found.

Test the storage behaviour around app restarts: (1) Add several bills, then force-quit the app immediately (within 2 seconds of saving the last bill). Reopen — are all bills there? AsyncStorage writes are asynchronous and there is a small window where data may not be flushed. (2) Add a bill, mark it as paid, close the app, reopen — is the paid status preserved correctly? (3) Change the default currency in Settings, close the app, reopen — is the setting remembered? Fix any cases where state is lost on restart.

Test BillMinder’s storage with unusual bill names: (1) A bill named with emoji: ‘U0001f4b3 Netflix’ — does it save and display correctly? (2) A very long name: 100 characters — does the bill card display it without breaking the layout? Add a character limit of 50 if it breaks. (3) A bill amount of $0 — is this valid? A zero-amount bill is unusual but should not crash anything. (4) A bill named with special characters: ‘AT&T / Phone & Internet’ — do the & and / characters cause any issues in storage or display? Fix any display or storage issues found.